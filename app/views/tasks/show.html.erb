<% content_for :title, "#{t('title.show_task')} - #{@task.title}" %>

<h1 class="text-danger"><%= t('title.show_task') %></h1>

<p id="notice"><%= notice %></p>

<p>
  <strong><%= Task.human_attribute_name(:title) %>:</strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:description) %>:</strong>
  <%= @task.description %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:status) %>:</strong>
  <%= @task.status ? 'Concluída' : 'Pendente' %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:created_at) %>:</strong>
  <%= @task.created_at %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:updated_at) %>:</strong>
  <%= @task.updated_at %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:completed_at) %>:</strong>
  <%= @task.completed_at %>
</p>

<p>
  <strong><%= Task.human_attribute_name(:deleted_at) %>:</strong>
  <%= @task.deleted_at %>
</p>

<%= link_to t('buttons.update_task'), edit_task_path(@task) %> |
<%= link_to t('buttons.delete_task'), task_path(@task), method: :delete, data: { confirm: t('messages.delete') } %> |
<%= link_to t('buttons.back'), tasks_path %>
