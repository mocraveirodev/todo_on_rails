<% content_for :title, t('title.new_task') %>

<h1 class="text-danger"><%= t('messages.new_task') %></h1>

<%= render 'form', task: @task %>

<%= link_to t('buttons.back'), tasks_path, class:"btn btn-secondary btn-lg" %>
