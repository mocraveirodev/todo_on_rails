<% content_for :title, "#{t('title.update_task')} - #{@task.title}" %>

<h1 class="text-danger"><%= t('title.update_task') %></h1>

<%= render 'form', task: @task %>

<%= link_to t('buttons.back'), tasks_path, class:"btn btn-secondary btn-lg" %>
